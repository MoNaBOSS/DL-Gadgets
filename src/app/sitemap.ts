import type { MetadataRoute } from "next";
import { getCategories, getProducts } from "@/lib/products";
import { siteConfig } from "@/lib/site";
export default async function sitemap(): Promise<MetadataRoute.Sitemap> { const [categories, products] = await Promise.all([getCategories(), getProducts()]); const staticPages = ["", "/shop", "/categories", "/about", "/contact", "/request-order"]; return [...staticPages.map((path) => ({ url: `${siteConfig.url}${path}`, lastModified: new Date(), changeFrequency: "weekly" as const, priority: path === "" ? 1 : .7 })), ...categories.map((item) => ({ url: `${siteConfig.url}/category/${item.slug}`, lastModified: new Date(), changeFrequency: "weekly" as const, priority: .8 })), ...products.map((item) => ({ url: `${siteConfig.url}/shop/${item.slug}`, lastModified: new Date(), changeFrequency: "weekly" as const, priority: .6 }))]; }

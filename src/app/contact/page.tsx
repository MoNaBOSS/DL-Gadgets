import type { Metadata } from "next";
import { AtSign, Mail, MapPin, MessageCircle, MessagesSquare } from "lucide-react";
import { Container } from "@/components/store";
import { siteConfig, whatsappLink } from "@/lib/site";

export const metadata: Metadata = { title: "Contact", description: "Contact DL Gadgets for product availability, ordering help, and support in Malta.", alternates: { canonical: "/contact" }, openGraph: { url: "/contact" } };
export default function ContactPage() { const email = process.env.NEXT_PUBLIC_CONTACT_EMAIL || "hello@dlgadgets.mt"; const details = [[Mail, "Email", email, `mailto:${email}`], [MessageCircle, "WhatsApp", "Fastest for product enquiries", whatsappLink()], [AtSign, "Instagram", "@dl_gadgetsmt", siteConfig.socials.instagram], [MessagesSquare, "Facebook", "DL Gadgets Malta", siteConfig.socials.facebook]]; return <section className="py-14 sm:py-18"><Container><div className="grid gap-10 lg:grid-cols-[.85fr_1.15fr]"><div><p className="text-xs font-bold uppercase tracking-[.22em] text-cyan-700">Get in touch</p><h1 className="mt-3 text-5xl font-black tracking-tight text-slate-950">Let&apos;s find the right tech.</h1><p className="mt-5 max-w-lg text-lg leading-8 text-slate-600">Ask about a listed device, current availability, or a product you cannot see in the catalog. DL Gadgets will confirm the next step directly.</p><p className="mt-6 flex items-center gap-2 text-sm font-bold text-slate-700"><MapPin className="size-5 text-cyan-600" />Serving customers across Malta</p></div><div className="grid gap-4 sm:grid-cols-2">{details.map(([Icon, title, text, href]) => { const DetailIcon = Icon as typeof Mail; return <a key={title as string} href={href as string} target={(href as string).startsWith("http") ? "_blank" : undefined} rel="noreferrer" className="rounded-2xl border border-slate-200 bg-white p-6 transition hover:-translate-y-1 hover:border-cyan-300 hover:shadow-lg"><DetailIcon className="size-6 text-cyan-600" /><h2 className="mt-5 font-black text-slate-950">{title as string}</h2><p className="mt-2 text-sm text-slate-600">{text as string}</p></a>; })}</div></div></Container></section>; }
